# トラブルシューティングガイド

このドキュメントでは、PDF編集ツールでよくある問題とその解決方法を説明します。

## 🔍 問題の診断

### デバッグツールの使用

1. **ブラウザのデベロッパーツールを開く**
   - Windows/Linux: `F12` または `Ctrl + Shift + I`
   - Mac: `Cmd + Option + I`

2. **コンソールタブを確認**
   - ライブラリの読み込み状態が表示されます
   - エラーメッセージを確認してください

3. **debug.htmlを使用**（ローカル環境のみ）
   ```
   http://localhost:8000/debug.html
   ```
   - ライブラリの読み込みテストができます
   - 各ライブラリの動作確認ができます

---

## 🐛 よくある問題と解決方法

### 問題1: ページが真っ白 / 何も表示されない

**症状:**
- ページを開いても何も表示されない
- ローディングアニメーションが表示されない

**原因:**
- JavaScriptライブラリの読み込み失敗
- Service Workerのキャッシュ問題
- ブラウザの互換性問題

**解決方法:**

#### 1-1. キャッシュをクリア
```
1. ブラウザの設定を開く
2. プライバシーとセキュリティ > 閲覧履歴データの削除
3. 「キャッシュされた画像とファイル」を選択
4. 「データを削除」をクリック
5. ページをリロード（Ctrl + F5 / Cmd + Shift + R）
```

#### 1-2. Service Workerをアンインストール
```
1. デベロッパーツール（F12）を開く
2. Applicationタブ（Chrome）またはStorageタブ（Firefox）をクリック
3. Service Workersを選択
4. 「Unregister」をクリック
5. ページをリロード
```

#### 1-3. サポートされているブラウザを使用
- Chrome 90+
- Firefox 88+
- Safari 14+
- Edge 90+

---

### 問題2: PDFファイルが読み込めない

**症状:**
- PDFをドラッグ&ドロップしてもエラーが出る
- 「PDFの読み込みに失敗しました」と表示される

**原因:**
- ファイルが破損している
- ファイルサイズが大きすぎる
- 暗号化されたPDF

**解決方法:**

#### 2-1. ファイルサイズを確認
```
推奨: 50MB以下
最大: 100MB

大きなファイルの場合:
1. 別のツールで圧縮
2. ページを分割
3. 画像の解像度を下げる
```

#### 2-2. PDFの暗号化を解除
```
1. Adobe Acrobatなどでパスワード保護を解除
2. 再度アップロード
```

#### 2-3. 別のPDFで試す
```
1. テスト用の小さなPDFを作成
2. 読み込みが成功するか確認
3. 特定のPDFだけの問題か確認
```

---

### 問題3: ライブラリ読み込みエラー

**症状:**
- コンソールに「pdfjsLib is not defined」などのエラー
- 機能ボタンをクリックしても反応しない

**原因:**
- CDNからのライブラリ読み込み失敗
- ネットワーク接続の問題
- CORS (Cross-Origin Resource Sharing) エラー

**解決方法:**

#### 3-1. ネットワーク接続を確認
```
1. インターネット接続を確認
2. ファイアウォール設定を確認
3. VPNを無効化して試す
```

#### 3-2. CDN接続を確認
デベロッパーツール > Networkタブで以下を確認:
```
✅ pdf.min.js: Status 200
✅ pdf-lib.min.js: Status 200
✅ tesseract.min.js: Status 200

❌ Status 404/403/CORS Error の場合:
   - 時間をおいて再試行
   - 別のネットワークで試す
```

#### 3-3. ローカルファイルとして開いている場合
```
file:/// プロトコルでは動作しません

解決策:
1. HTTPサーバーを起動
   python -m http.server 8000

2. ブラウザで開く
   http://localhost:8000
```

---

### 問題4: 編集機能が動作しない

**症状:**
- テキスト追加ボタンをクリックしてもモーダルが開かない
- 画像挿入ができない
- エラーメッセージが表示される

**原因:**
- pdf-libライブラリの読み込み失敗
- PDFの複雑な構造
- メモリ不足

**解決方法:**

#### 4-1. ライブラリ状態を確認
デベロッパーツールのコンソールで:
```javascript
console.log(typeof PDFLib);
// 出力: "object" であればOK
// 出力: "undefined" の場合はライブラリ未読み込み
```

#### 4-2. シンプルなPDFで試す
```
1. 新しい空のPDFを作成
2. 編集機能をテスト
3. 問題が解決するか確認
```

#### 4-3. ブラウザのメモリを解放
```
1. 他のタブを閉じる
2. ブラウザを再起動
3. PCのメモリ使用状況を確認
```

---

### 問題5: OCRが動作しない

**症状:**
- OCRボタンをクリックしても何も起こらない
- 「OCR処理に失敗しました」とエラーが出る

**原因:**
- Tesseract.jsの読み込み失敗
- 処理に時間がかかりすぎる
- 画像品質が悪い

**解決方法:**

#### 5-1. 処理時間を確保
```
OCRは時間がかかります:
- 1ページ: 5-30秒
- 複数ページ: さらに時間がかかる

解決策:
- プログレスバーを確認
- 処理完了まで待つ
- 小さいページ範囲で試す
```

#### 5-2. 画像品質を向上
```
OCR精度を上げるには:
✅ 高解像度スキャン（300dpi以上）
✅ コントラストが高い
✅ ノイズが少ない
✅ テキストが水平

❌ 避けるべき:
- 手書き文字
- 低解像度画像
- 傾いたテキスト
- 複雑な背景
```

---

### 問題6: GitHub Pagesで動作しない

**症状:**
- ローカルでは動作するがGitHub Pagesで動作しない
- 404エラー
- CSSやJSが読み込まれない

**原因:**
- パスの問題
- GitHub Pages設定の問題

**解決方法:**

#### 6-1. パスを確認
```html
<!-- ❌ 絶対パス (GitHub Pagesでは動作しない) -->
<link rel="stylesheet" href="/css/style.css">

<!-- ✅ 相対パス (推奨) -->
<link rel="stylesheet" href="css/style.css">
<!-- または -->
<link rel="stylesheet" href="./css/style.css">
```

#### 6-2. GitHub Pages設定を確認
```
1. Settings > Pages
2. Source: GitHub Actions または Deploy from a branch
3. Branch: main (または master)
4. Folder: / (root)
5. Save をクリック
```

#### 6-3. デプロイ状況を確認
```
1. Actionsタブをクリック
2. 最新のワークフローを確認
3. エラーログを確認
```

---

## 💡 パフォーマンス最適化

### 大きなPDFファイルを扱う場合

```
1. ページ数を減らす
   - 必要なページだけを抽出
   - PDF分割機能を使用

2. 画像を圧縮
   - PDF圧縮機能を使用
   - 別ツールで画像最適化

3. ブラウザのメモリを確保
   - 他のタブを閉じる
   - 不要な拡張機能を無効化
```

### ブラウザ推奨設定

```
Chrome:
- ハードウェアアクセラレーションを有効化
- 設定 > 詳細設定 > システム

Firefox:
- WebGL を有効化
- about:config > webgl.disabled = false

Safari:
- 実験的機能を有効化
- 開発 > Experimental Features
```

---

## 🔧 開発者向けデバッグ

### ローカル開発サーバーの起動

```bash
# Python 3
python -m http.server 8000

# Python 2
python -m SimpleHTTPServer 8000

# Node.js (http-server)
npx http-server -p 8000

# PHP
php -S localhost:8000
```

### コンソールでのデバッグ

```javascript
// ライブラリ確認
console.log('PDF.js:', typeof pdfjsLib);
console.log('pdf-lib:', typeof PDFLib);
console.log('Tesseract:', typeof Tesseract);

// PDFビューアー状態
console.log('PDF Viewer:', pdfViewer);
console.log('Current Page:', pdfViewer.currentPage);
console.log('Total Pages:', pdfViewer.totalPages);

// エディター状態
console.log('PDF Editor:', pdfEditor);
```

### ネットワークリクエストの確認

```
1. デベロッパーツール > Network タブ
2. ページをリロード
3. 各リソースのステータスを確認:
   - 200: 成功
   - 304: キャッシュから読み込み
   - 404: ファイルが見つからない
   - 403: アクセス拒否
   - CORS error: クロスオリジンエラー
```

---

## 📞 それでも解決しない場合

### サポートを受ける

1. **GitHub Issues**
   ```
   https://github.com/masaspc/PDFEdit/issues

   以下の情報を含めてください:
   - ブラウザとバージョン
   - OS
   - エラーメッセージ
   - 再現手順
   - スクリーンショット
   ```

2. **コンソールログを確認**
   ```
   デベロッパーツール（F12）を開く
   → Console タブ
   → エラーメッセージをコピー
   ```

3. **ネットワークログを確認**
   ```
   デベロッパーツール（F12）を開く
   → Network タブ
   → 失敗したリクエストを確認
   ```

---

## 🎯 クイックチェックリスト

問題が発生したら、以下を順番に確認:

- [ ] サポートされているブラウザを使用している
- [ ] インターネット接続が安定している
- [ ] ブラウザのキャッシュをクリアした
- [ ] Service Workerをアンインストールした
- [ ] デベロッパーツールでエラーを確認した
- [ ] ファイルサイズが100MB以下
- [ ] PDFが暗号化されていない
- [ ] HTTPサーバー経由でアクセスしている（file:///ではない）
- [ ] 他のPDFファイルでも同じ問題が起きる
- [ ] ブラウザを再起動した

---

**最終更新**: 2025年11月17日
